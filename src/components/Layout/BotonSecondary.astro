---
const { texto, href, clase } = Astro.props;
---

<a
	class={`xl:w-fit w-full text-center px-3 py-3 rounded-xl bg-secondary hover:bg-black hover:text-white border border-secondary transition-colors ${clase}`}
	href={href}>{texto}</a
>
