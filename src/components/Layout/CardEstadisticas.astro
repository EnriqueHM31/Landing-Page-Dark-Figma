---
const { numero, title, i } = Astro.props;
---

<div
	class={`flex flex-col gap-3 bg-primary px-3 py-4 xl:mt-7 rounded-2xl ${i === 2 ? 'col-span-2' : ''} xl:flex-1 items-center xl:items-start `}
	data-aos="zoom-in"
	data-aos-delay={`${i * 100 + 400}`}
>
	<p class="text-3xl font-bold">{numero}</p>
	<p class="text-sm text-white/50 text-center">{title}</p>
</div>
